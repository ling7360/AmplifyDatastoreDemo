"use strict";

/* 
1、该文件是用于创建一个为Counter组件服务的reducer，reducer的本质就是一个函数
2、reducer函数会接到两个参数，分别为：之前的状态(previousState)，动作对象(action)
*/
function restaurantReducer(prevState, action) {
  var type = action.type,
      data = action.data;

  switch (key) {
    case 'setRestaurant':
      return data;

    default:
      break;
  }
}